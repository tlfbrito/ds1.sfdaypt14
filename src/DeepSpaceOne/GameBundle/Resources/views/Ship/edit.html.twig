{% extends '::base.html.twig' %}

{% set active_section = 'ships' %}

{% block body -%}
    <div class="page-header">
        <h1>Manage Ship</h1>
    </div>

    {% form_theme edit_form 'DeepSpaceOneGameBundle:Ship:ship-form.html.twig' %}
    {{ form(edit_form) }}

    <ul class="record_actions list-inline">
        <li><a href="{{ path('ships') }}" class="btn btn-default"><span class="glyphicon glyphicon-list"></span> Back to the list</a></li>
        <li>{{ form(delete_form) }}</li>
    </ul>
{% endblock %}
